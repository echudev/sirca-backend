version: "2"
sql:
  - engine: "postgresql"
    schema: "./db/schema.sql"
    queries: "./db/queries.sql"
    gen:
      go:
        package: "db"
        out: "db" # Especifica explícitamente el directorio de salida
        sql_package: "pgx/v5"
